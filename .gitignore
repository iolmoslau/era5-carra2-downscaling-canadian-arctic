# Scratch / downloaded data (never commit large reanalysis files)
data_acquisition/_scratch_data/
*.nc
*.grib
*.grib2
*.zarr/
*.idx

# Kaggle dataset upload staging (generated; the .zarr is already ignored above)
dataset-metadata.json

# Figures / outputs
*.png
*.pdf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
env/

# Credentials (CDS key lives in ~/.cdsapirc, but guard against copies)
.cdsapirc
*.key

# SLURM / cluster logs
logs/
slurm-*.out
*.out
*.err

# OS / editor
.DS_Store
.vscode/
.idea/
